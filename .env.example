GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
# Paste a new valid Gemini API key in .env, not in this example file.
# Optional legacy fallback. Keep disabled unless you intentionally use api_key.json.
ALLOW_LEGACY_API_KEY_JSON=false
MAIL_USERNAME=
MAIL_PASSWORD=
